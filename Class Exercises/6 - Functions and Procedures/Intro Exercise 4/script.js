// 1. Write a procedure to display a welcome message
function displayWelcomeMessage() {
    // Fill in here
}

// 2. Write a function to calculate the average grade
function calculateAverage(grades) {
    // Fill in here
    // Hint: Use a loop or the .reduce() method
}

// 3. Write a function to determine the letter grade
function getLetterGrade(average) {
    // Fill in here
    // Hint: Use if-else or a switch statement
}

// 4. Write a procedure to display the result
function displayResult(average, letterGrade) {
    // Fill in here
    // Example output: "The average grade is 85. The letter grade is B."
}

// Main Program
const grades = [90, 85, 78, 92, 88]; // Example grades
displayWelcomeMessage();
const average = calculateAverage(grades);
const letterGrade = getLetterGrade(average);
displayResult(average, letterGrade);
